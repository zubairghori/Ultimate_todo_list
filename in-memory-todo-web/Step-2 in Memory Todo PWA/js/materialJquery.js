
// side navbar

$(document).ready(function(){
    $('.sidenav').sidenav();
  });

// Floating Action Button

$(document).ready(function () {
    $('.floating-action-btn').floatingActionButton();
});


// Add Task Floating Button with tooltipped property

$(document).ready(function () {
    $('.fixed-action-btn').floatingActionButton();
});

$(document).ready(function () {
    $('.tooltipped').tooltip();
});